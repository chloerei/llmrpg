<div id="<%= dom_id room %>" class="list-row hover:bg-base-200">
  <div class="list-col-grow">
    <h3 class="text-lg font-medium">
      <a href="<%= room_path(room) %>" class="block after:absolute after:inset-0">
        <%= room.name %>
      </a>
    </h3>
  </div>
  <div class="flex-none">
    <%= render "avatar_group", room: room %>
  </div>
</div>
